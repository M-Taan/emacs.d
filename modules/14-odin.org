* Odin
=odin-mode=

#+begin_src emacs-lisp
 ;; (straight-use-package
 ;;  '(odin-mode :type git
 ;;              :host sourcehut
 ;;              :repo "mgmarlow/odin-mode"))

 ;; Enable odin-mode and configure OLS as the language server
 ;; (use-package odin-mode
 ;;   :mode ("\\.odin\\'" . odin-mode)
 ;;   :hook (odin-mode . lsp))

 ;; ;; Set up OLS as the language server for Odin, ensuring lsp-mode is loaded first
 ;; (with-eval-after-load 'lsp-mode
 ;;   (setq-default lsp-auto-guess-root t)
 ;;   (setq lsp-language-id-configuration (cons '(odin-mode . "odin") lsp-language-id-configuration))

 ;;   (lsp-register-client
 ;;    (make-lsp-client :new-connection (lsp-stdio-connection (concat user-emacs-directory "/odin-lsp/ols"))
 ;;                     :major-modes '(odin-mode)
 ;;                     :server-id 'ols
 ;;                     :multi-root t)))

 ;; (add-hook 'odin-mode-hook #'lsp)
#+end_src
