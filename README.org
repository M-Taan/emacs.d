* Emacs Configuration
This file is a stub. All configuration lives in =modules/= and is loaded by =init.el=.

** Package management is handled by =straight.el=, bootstrapped in =init.el=.
** All =use-package= forms use =straight.el= by default via =straight-use-package-by-default=.
** Each module in =modules/= is tangled and loaded independently by =init.el=.
